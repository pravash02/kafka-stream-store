FROM python:3.11-slim

WORKDIR /app

# Copy package metadata and source from repository root
COPY pyproject.toml .
COPY kafka/ ./kafka/

# Install dependencies
RUN pip install --no-cache-dir -e .

# Run producer
CMD ["python", "-m", "kafka.producer"]
